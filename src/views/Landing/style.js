import styled from 'styled-components';

const Landing = styled.div`
  /* min-height: 70vh; */
  padding-top: 120px;
  display: flex;
  justify-content: center;
  align-items: center;
  .hero-text {
    max-width: 540px;
    font-weight: 900;
    letter-spacing: 1px;
  }
`;

export const Styled = {
  Landing
};
