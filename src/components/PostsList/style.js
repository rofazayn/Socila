import styled from 'styled-components';

const PostsList = styled.div`
  display: flex;
  padding-top: 16px;
  padding-bottom: 50vh;
  flex-direction: column;
`;

export const Styled = {
  PostsList
};
