import styled from 'styled-components';

const SettingsBox = styled.div`
  max-width: 460px;
  width: 100%;
  min-height: 500px;
  box-shadow: 0 0 15px rgba(black, 0.05);
`;

export const Styled = {
  SettingsBox,
};
